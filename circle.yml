machine:
  node:
    version: 4.2.1

dependencies:
  pre:
    - echo "//registry.npmjs.org/:_authToken=$NPM_AUTH_TOKEN" > ~/.npmrc
deployment:
  npm:
    branch: master
    commands:
      - npm run ./node_modules/.bin/publish
