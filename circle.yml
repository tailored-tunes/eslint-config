machine:
  node:
    version: 9.6.1

dependencies:
  pre:
    - echo "//registry.npmjs.org/:_authToken=$NPM_AUTH_TOKEN" > ~/.npmrc
deployment:
  npm:
    branch: master
    commands:
      - ./node_modules/.bin/publish
